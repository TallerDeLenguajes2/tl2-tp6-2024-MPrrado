@using EspacioProductos
@model Producto

<h1 class="text-center"> MODIFICAR PRODCUTO</h1>
<section class="d-flex justify-content-center">
    <form asp-action="Index" asp-controller="Productos" method="post" class="col-6">
        @* @{
            var productoRepository = new ProductoRepository();
            produc
        } *@
        <section>
            <input asp-for="@Model.IdProducto"hidden>
        </section>
        <section>
            <label asp-for="@Model.Descripcion" class="from-label"></label>
            <input asp-for="@Model.Descripcion" class="form-control">
        </section>
        
        <section>
            <label asp-for="@Model.Precio" class="from-label"></label>
            <input asp-for="@Model.Precio" class="form-control">
        </section>

        <section class="mt-3">
            <button type="submit" class="btn btn-primary" asp-controller="Productos" asp-action="ModificarProducto"> Modificar</button>
        </section>
    </form>
</section>